@page "/countries"

@*
<div class="mb-3">
    <a class="btn btn-primary" href="/countries/create">@Localizer["New"] @Localizer["Country"]</a>
</div>


<GenericList MyList="Countries">
    <Body>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>@Localizer["Country"]</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var country in Countries!)
                {
                    <tr>
                        <td>
                            @country.Name
                        </td>
                        <td>
                            <a class="btn btn-warning" href="/countries/edit/@country.Id">@Localizer["Edit"]</a>
                            <button class="btn btn-danger" @onclick=@(() => DeleteAsync(country))>@Localizer["Delete"]</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </Body>
</GenericList
*@

<MudMainContent>
    <MudContainer MaxWidth="MaxWidth.Medium">
   

        <MudButton Href="/countries/create" Variant="Variant.Filled" Color="Color.Primary">
            @Localizer["New"]  <!-- Puedes personalizar el texto aquí -->
        </MudButton>


   <MudTable Items="@Countries"
       @ref="_table"
       Dense="@dense" Hover="@hover" Bordered="@bordered" Striped="@striped" 
       Filter="FilterFunc1" @bind-SelectedItem="selectedItem1">
    <ToolBarContent>
                <MudText Typo="Typo.h6">@Localizer["Countries"]</MudText>
        <MudSpacer />
        <MudTextField @bind-Value="searchString1" Placeholder="Buscar..." Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
    </ToolBarContent>
    <HeaderContent>
        <MudTh>Código</MudTh>
        <MudTh>Pais</MudTh>
        <MudTh>@Localizer["Actions"]</MudTh> 
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="Id">@context.Id</MudTd>
        <MudTd DataLabel="Name">@context.Name</MudTd>

          <MudTd DataLabel="@Localizer["Actions"]">
         
        </MudTd>



          
    </RowTemplate>
    <PagerContent>
                @* <MudPagination SelectedChanged="PageChanged" Count="@((_table.GetFilteredItemsCount() + _table.RowsPerPage - 1) / _table.RowsPerPage)" Class="pa-4" />*@
               <MudTablePager />
    </PagerContent>
</MudTable>


    </MudContainer>
</MudMainContent>


